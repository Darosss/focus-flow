export type TimeData = {
  name: string;
  time: number;
  category: string;
  color: string;
};

export type CurrentActivityData = {
  name: string;
  window: string;
  duration: number;
  category: string;
};
